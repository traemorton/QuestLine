<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        h1 {
            color: #333;
        }
        .container {
            padding: 20px;
            border: 1px solid #ddd;
            background-color: #f9f9f9;
            border-radius: 8px;
            max-width: 500px;
            margin: auto;
            text-align: center;
        }
        .profile-pic {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 10px;
        }
        .info {
            margin-top: 10px;
            font-size: 18px;
            color: #555;
        }
        .logout-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 15px;
            color: white;
            background-color: #dc3545;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
        }
        .logout-btn:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
    <%- include('partials/profile-header', { user: user }) %>

    <%- include('partials/header') %>

    <div class="container">
        <h1>Welcome, <%= user.name %>!</h1>
        <img src="<%= user.profilePicture %>" alt="Profile Picture" class="profile-pic">
        <p class="info"><strong>Username:</strong> <%= user.username %></p>
        <p class="info"><strong>Email:</strong> <%= user.email %></p>
        <p class="info"><strong>Bio:</strong> <%= user.bio || 'No bio available' %></p>
        <p class="info"><strong>Role:</strong> <%= user.role %></p>
        <p class="info"><strong>Location:</strong> <%= user.location %></p>
        <p class="info"><strong>Online Status:</strong> <%= user.onlineStatus ? 'Online' : 'Offline' %></p>
        <p class="info"><strong>Last Login:</strong> <%= new Date(user.lastLogin).toLocaleString() %></p>
        <p class="info"><strong>Preferences:</strong> Theme - <%= user.preferences.theme %>, Notifications - <%= user.preferences.notifications ? 'Enabled' : 'Disabled' %></p>
        <a href="/edit-profile" class="btn">Edit Profile</a>
        <a href="/logout" class="logout-btn">Logout</a>
    </div>

    <%- include('partials/footer') %>
</body>
</html>